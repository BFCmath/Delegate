# Core ML/AI Libraries
torch>=2.0.0
transformers>=4.37.0
google-generativeai>=0.3.0
huggingface-hub>=0.20.0
unsloth  # Optional: 2x faster inference + 4-bit quantization for Qwen models
vllm>=0.4.0  # Required for fast inference in offline LLM and SLM experiments

# Quantization Libraries
bitsandbytes>=0.43.0  # Required for 4-bit/8-bit quantization (BitsAndBytes)
auto-gptq>=0.7.0  # Required for GPTQ quantization
autoawq>=0.2.0  # Required for AWQ quantization
accelerate>=0.25.0  # Required for BitsAndBytes integration

# Data Processing
pandas>=2.0.0
datasets>=4.0.0

# Search Tools (for Deep Research experiments)
duckduckgo-search>=4.0.0  # Free search, no API key needed
tavily-python  # Optional: Higher quality search, requires TAVILY_API_KEY

# Utilities
python-dotenv>=1.0.0
tavily-python

# Visualization (optional, for analyze_results.py)
matplotlib>=3.7.0
seaborn>=0.12.0

# Note: Install with:
# pip install -r requirements.txt
#
# For GPU support (CUDA):
# pip install torch --index-url https://download.pytorch.org/whl/cu118